'use strict';

// pagfiguration : pagfig - pagfig.env.PORT;
// const config = require('./configuration');

const math = {
    PORT: 3000,
    HOSTNAME: 'localhost',
    NODE_ENV: 'dev',
};

module.exports = math;
